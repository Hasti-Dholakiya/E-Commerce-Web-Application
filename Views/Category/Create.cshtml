@model Day6.Models.Category
@{
    ViewBag.Title = "Add Category";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background-color: #f1f3f5;
            font-family: 'Segoe UI', sans-serif;
        }

        .hero {
            background-image: url('https://source.unsplash.com/1600x400/?category,technology,books');
            background-size: cover;
            background-position: center;
            height: 250px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-shadow: 1px 1px 5px rgba(0,0,0,0.6);
        }

            .hero h1 {
                background-color: rgba(0,0,0,0.6);
                padding: 15px 30px;
                border-radius: 10px;
            }

        .form-container {
            max-width: 500px;
            margin: -70px auto 40px;
            background-color: #ffffff;
            padding: 35px 30px;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0,0,0,0.1);
        }

            .form-container h4 {
                margin-bottom: 25px;
                font-weight: 600;
                color: #343a40;
            }

        .btn-lg {
            padding: 0.75rem 1.25rem;
        }

        .form-floating label {
            font-weight: 500;
        }

        .text-danger {
            font-size: 0.875rem;
        }

        .d-grid .btn + .btn {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="hero mb-5">
        <h1>@ViewBag.Title</h1>
    </div>

   
    <div class="form-container">
        <h4 class="text-center">Add a New Category</h4>

        @using (Html.BeginForm())
        {
            @Html.HiddenFor(m => m.CategoryId)

            <div class="form-floating mb-3">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Category Name", autocomplete = "off" })
                @Html.LabelFor(m => m.Name)
                @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-success btn-lg">Save</button>
                <a href="@Url.Action("Index", "Category")" class="btn btn-outline-secondary btn-lg">Cancel</a>
            </div>
        }
    </div>

    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
